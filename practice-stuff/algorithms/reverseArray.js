        //Array index starts from 0, so here 20 = 3
        var exampleArray = [ 10, 15, 54, 20 ];

        //Using .length to find the size of the array then subtracting 1 
        for(var i=exampleArray.length - 1; i>=0; i--) {
            console.log(exampleArray[i]);
        };